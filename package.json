{
  "name": "staff-coach-portal",
  "version": "0.12.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check:architecture": "node scripts/validate-architecture.mjs",
    "check": "npm run check:syntax && npm run check:architecture && npm run check:print && npm run check:match-calendar && npm run build",
    "check:print": "node scripts/check-print-regression.mjs",
    "check:match-calendar": "node scripts/check-match-calendar.mjs"
  },
  "devDependencies": {
    "supabase": "^2.111.0",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8"
  }
}
